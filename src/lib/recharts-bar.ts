/** Shared recharts bar hover styling (no full-height tooltip cursor band). */
export const BAR_ACTIVE_BAR = {
  stroke: "rgba(139, 92, 246, 0.8)",
  strokeWidth: 2,
  fill: "rgba(139, 92, 246, 0.9)",
  radius: 4,
} as const;

export const BAR_CHART_DATA_ATTR = "data-bar-chart";
